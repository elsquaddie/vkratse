# 🚀 Установка и Деплой

Пошаговая инструкция по настройке бота "Что было в чате" (@chto_bilo_v_chate_bot)

---

## 📋 Предварительные требования

1. **Telegram Bot Token**
   - Создай бота через [@BotFather](https://t.me/botfather)
   - Команда: `/newbot`
   - Сохрани токен

2. **Anthropic API Key**
   - Зарегистрируйся на [console.anthropic.com](https://console.anthropic.com/)
   - Создай API key
   - Убедись что есть кредиты на аккаунте

3. **Supabase Database**
   - Создай проект на [supabase.com](https://supabase.com/)
   - Сохрани URL и anon/public key

4. **Vercel Account**
   - Зарегистрируйся на [vercel.com](https://vercel.com/)
   - Подключи GitHub аккаунт

---

## 🗄️ Шаг 1: Настройка Базы Данных

### 1.1. Создать таблицы

1. Открой Supabase Dashboard → SQL Editor
2. Выполни скрипт `sql/init_tables.sql`:
   - Скопируй весь файл
   - Вставь в SQL Editor
   - Нажми "Run"

### 1.2. Заполнить базовые личности

1. В том же SQL Editor
2. Выполни скрипт `sql/seed_personalities.sql`:
   - Скопируй весь файл
   - Вставь в SQL Editor
   - Нажми "Run"

3. Проверь что личности созданы:
```sql
SELECT name, display_name FROM personalities ORDER BY id;
```

Должны быть 7 личностей:
- neutral (Нейтральный) ← дефолт
- bydlan (Быдлан)
- philosopher (Философ)
- gopnik (Гопник)
- oligarch (Олигарх)
- comedian (Стендапер)
- scientist (Учёный)

---

## ⚙️ Шаг 2: Настройка Переменных Окружения

### 2.1. Локально (для разработки)

1. Скопируй `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Заполни переменные в `.env`:
```bash
# Telegram
TELEGRAM_BOT_TOKEN=твой_токен_от_botfather

# Claude API
ANTHROPIC_API_KEY=sk-ant-твой_ключ

# Supabase
SUPABASE_URL=https://твой-проект.supabase.co
SUPABASE_KEY=твой_anon_key

# Security
SECRET_KEY=случайная_строка_для_hmac
```

### 2.2. Для Vercel

Не коммить `.env` в git! Настроим переменные через Vercel позже.

---

## 🚀 Шаг 3: Деплой на Vercel

### 3.1. Подготовка репозитория

1. Инициализируй git (если ещё не сделано):
```bash
git init
git add .
git commit -m "Initial commit"
```

2. Создай репозиторий на GitHub

3. Залей код:
```bash
git remote add origin git@github.com:твой-username/vkratse.git
git branch -M main
git push -u origin main
```

### 3.2. Деплой через Vercel

1. Открой [vercel.com/new](https://vercel.com/new)

2. Импортируй репозиторий с GitHub

3. Настройки проекта:
   - **Framework Preset**: Other
   - **Root Directory**: ./
   - **Build Command**: (оставь пустым)
   - **Output Directory**: (оставь пустым)

4. Добавь переменные окружения (Environment Variables):
   ```
   TELEGRAM_BOT_TOKEN = твой_токен
   ANTHROPIC_API_KEY = sk-ant-твой_ключ
   SUPABASE_URL = https://твой-проект.supabase.co
   SUPABASE_KEY = твой_anon_key
   SECRET_KEY = случайная_строка_минимум_32_символа
   ```

5. Нажми **Deploy**

6. Дождись завершения деплоя (1-2 минуты)

7. Сохрани URL (например: `https://vkratse.vercel.app`)

---

## 🔗 Шаг 4: Настройка Webhook

### 4.1. Установить webhook

Выполни команду (замени `<TOKEN>` и `<URL>`):

```bash
curl "https://api.telegram.org/bot<ТВОЙ_BOT_TOKEN>/setWebhook?url=<VERCEL_URL>/api/index"
```

Пример:
```bash
curl "https://api.telegram.org/bot123456:ABC-DEF/setWebhook?url=https://vkratse.vercel.app/api/index"
```

Ответ должен быть:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

### 4.2. Проверить webhook

```bash
curl "https://api.telegram.org/bot<ТВОЙ_BOT_TOKEN>/getWebhookInfo"
```

Должен вернуться твой Vercel URL и `pending_update_count: 0`.

---

## ✅ Шаг 5: Тестирование

### 5.1. Проверка в Telegram

1. Найди своего бота в Telegram (@chto_bilo_v_chate_bot)

2. Отправь `/start`

3. Должен ответить приветствием

4. Создай тестовую группу

5. Добавь бота в группу

6. Напиши несколько сообщений

7. Выполни `/суть`

8. Бот должен сгенерировать саммари!

### 5.2. Проверка команд

Протестируй каждую команду:

```
/start - приветствие
/help - справка
/суть - саммари (в группе)
/суть 2ч - саммари за 2 часа
/суть сегодня - саммари с начала дня
/личность - выбор стиля AI
/рассуди Кто прав? - судейство
```

### 5.3. Проверка личностей

1. Выполни `/личность`

2. Попробуй выбрать разные стили:
   - 🎓 Нейтральный
   - 🏭 Быдлан
   - 🧙 Философ
   - и т.д.

3. После смены личности выполни `/суть` и проверь что стиль изменился

4. Попробуй создать свою личность через кнопку "➕ Создать свою"

---

## 🔧 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**
1. Проверь webhook:
```bash
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

2. Проверь логи Vercel:
   - Открой проект в Vercel
   - Вкладка "Deployments"
   - Кликни на последний деплой
   - "Runtime Logs"

3. Проверь переменные окружения в Vercel

### Проблема: Ошибка "Личность не найдена"

**Решение:**
1. Проверь что выполнил `sql/seed_personalities.sql`
2. Проверь в Supabase:
```sql
SELECT * FROM personalities;
```

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверь SUPABASE_URL и SUPABASE_KEY
2. Убедись что используешь **anon/public** key, а не service_role key
3. Проверь что таблицы созданы в правильной схеме (public)

### Проблема: Rate limit или cooldown

**Решение:**
Это нормально! Настройки защиты от спама:
- Cooldown между `/суть`: 60 секунд
- Rate limit: 10 запросов за 60 секунд

Можно изменить в переменных окружения:
```
COOLDOWN_SECONDS=30
RATE_LIMIT_REQUESTS=20
```

---

## 📊 Мониторинг

### Vercel Logs

Проверяй логи в Vercel Dashboard:
- Ошибки
- Время ответа
- Количество запросов

### Supabase Analytics

В Supabase Dashboard можешь посмотреть:
- Количество сообщений в БД
- Количество пользователей
- Кастомные личности

Пример запроса:
```sql
-- Статистика по событиям
SELECT event_type, COUNT(*)
FROM analytics
GROUP BY event_type
ORDER BY COUNT(*) DESC;

-- Топ пользователей
SELECT user_id, COUNT(*) as events
FROM analytics
GROUP BY user_id
ORDER BY events DESC
LIMIT 10;
```

---

## 🔄 Обновление Бота

### При изменении кода:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически передеплоит бот!

### При изменении БД:

1. Создай новый SQL скрипт в `sql/migrations/`
2. Выполни в Supabase SQL Editor
3. Задокументируй изменения

---

## 📝 Полезные Команды

### Проверка webhook
```bash
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

### Удаление webhook
```bash
curl "https://api.telegram.org/bot<TOKEN>/deleteWebhook"
```

### Получение информации о боте
```bash
curl "https://api.telegram.org/bot<TOKEN>/getMe"
```

### Локальное тестирование
```bash
python api/index.py
# Бот запустится на http://localhost:8000
# Для тестирования используй ngrok:
ngrok http 8000
```

---

## 🎉 Готово!

Бот настроен и работает!

Теперь можешь:
1. Добавить его в свои групповые чаты
2. Тестировать разные команды
3. Настраивать личности
4. Собирать фидбэк от друзей

---

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи в Vercel
2. Проверь данные в Supabase
3. Проверь переменные окружения
4. Создай issue в GitHub репозитории
