# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# 🤖 Что было в чате - Telegram Bot

## 📋 О проекте

**Telegram бот для AI-саммаризации групповых чатов с множественными личностями.**

### Технологический стек:
- **Backend**: Python 3.11+, Vercel Serverless Functions
- **Database**: Supabase (PostgreSQL)
- **AI**: Claude API (Anthropic)
- **Bot Framework**: python-telegram-bot
- **Deployment**: Vercel (webhook mode)

### Ключевые особенности:
- Множественные личности AI (7 базовых + кастомные)
- Прямое общение с ботом (direct chat) в выбранной личности
- Саммаризация чатов за разные периоды
- Судейство споров через /rassudi
- Выбор чатов в личных сообщениях
- Inline-меню для быстрой навигации
- Автоудаление старых сообщений (3 дня)
- Безопасность данных (проверки членства, HMAC, sanitization)

---

## 🗺️ CUSTOMER JOURNEY MAP (CJM)

### Общая философия UX:
1. **Нет дефолтной личности** - пользователь всегда делает осознанный выбор
2. **Интригующие названия** - личности с яркими характерами
3. **Inline-меню** - быстрый доступ ко всем функциям
4. **Контекст 3 дня** - история сообщений сохраняется автоматически
5. **Подсказки везде** - пользователь всегда знает, что делать дальше

---

### Путь 1: Личный чат (Direct Chat с ботом)

#### 1.1 Первый контакт
**Триггер:** Пользователь нажимает "Start" или пишет /start

**Флоу:**
```
[Пользователь] → /start
    ↓
[Бот] → Приветственное сообщение + Inline-меню
    ├─ 💬 Общаться напрямую
    ├─ 👥 Добавить в групповой чат
    └─ 🎭 Настроить личность
```

**Реализация:**
- `modules/commands.py:start()` - отправляет приветствие
- Inline-меню через `InlineKeyboardMarkup` с callback_query
- HMAC подпись для всех callback_data

**Текст приветствия:**
```
👋 Привет! Я бот с множественными личностями.

Я могу:
• Общаться с тобой в разных стилях
• Саммаризировать групповые чаты
• Рассуживать споры

Что будем делать?
```

#### 1.2 Выбор "Общаться напрямую"
**Триггер:** Клик на кнопку "💬 Общаться напрямую"

**Флоу:**
```
[Клик] → callback_query: "direct_chat"
    ↓
[Бот] → Меню выбора личности (7 базовых + кастомные)
    ├─ 🏭 Быдлан
    ├─ 🧙 Философ
    ├─ 👟 Гопник
    ├─ 💼 Олигарх
    ├─ 😂 Стендапер
    ├─ 🔬 Учёный
    ├─ 🎓 Нейтральный
    ├─ [Кастомные личности пользователя]
    └─ ➕ Создать свою личность
```

**Реализация:**
- `modules/direct_chat.py:show_personality_selection()` - новый модуль
- Получает все личности из БД (базовые + пользовательские)
- Отображает в 2 колонки для компактности

#### 1.3 Выбор личности
**Триггер:** Клик на конкретную личность

**Флоу:**
```
[Клик] → callback_query: "select_personality:bydlan"
    ↓
[БД] → Сохранить выбор в user_settings
    ↓
[Бот] → Персональное приветствие от личности
```

**Примеры приветствий:**

**Быдлан:**
```
Йоу, братан! Ну че, приехали? Че надо?
Не стой как столб, давай базарь чё к чему! 🏭
```

**Философ:**
```
Приветствую тебя, путник. Ты пришёл в поисках истины,
или просто заблудился в лабиринте бытия? 🧙
```

**Реализация:**
- `modules/direct_chat.py:set_personality()` - сохранение выбора
- `services/db_service.py:update_user_personality()` - БД операция
- Приветствия хранятся в таблице `personalities.greeting_message`
- Если `greeting_message` NULL → генерируем через Claude API

#### 1.4 Общение с ботом
**Триггер:** Пользователь пишет любое сообщение

**Флоу:**
```
[Сообщение] → "Привет, как дела?"
    ↓
[БД] → Сохранить сообщение в messages
    ↓
[БД] → Получить последние N сообщений (контекст)
    ↓
[Claude API] → Генерация ответа в стиле личности
    ↓
[Бот] → Ответ пользователю
    ↓
[БД] → Сохранить ответ бота в messages
```

**Контекст и лимиты:**
- Храним последние 3 дня сообщений (автоочистка)
- Для каждого запроса отправляем последние 20-30 сообщений
- При превышении лимита токенов → сообщение пользователю

**Реализация:**
- `modules/direct_chat.py:handle_message()` - обработчик
- `services/ai_service.py:generate_chat_response()` - генерация
- `services/db_service.py:get_chat_history()` - получение истории

#### 1.5 Смена личности
**Триггер:** Команда /lichnost или кнопка в меню

**Флоу:**
```
[Команда] → /lichnost
    ↓
[Бот] → Меню личностей (как в 1.2)
    ↓
[Выбор] → Новая личность
    ↓
[Бот] → "Личность изменена на [название]. История сохранена."
    ↓
[Дальнейшее общение] → В новой личности с сохранением контекста
```

**Реализация:**
- История НЕ очищается при смене личности
- Просто меняется system_prompt для Claude API
- Можно добавить уведомление в контекст: "Пользователь сменил личность на X"

#### 1.6 Создание кастомной личности
**Триггер:** Клик на "➕ Создать свою личность"

**Флоу:**
```
[Клик] → callback_query: "create_personality"
    ↓
[Бот] → "Опиши стиль личности (10-1500 символов):"
    ↓
[Пользователь] → "Грубый математик, который объясняет через формулы"
    ↓
[Валидация] → Sanitization + проверка запрещенных слов
    ↓
[Бот] → "Дай имя личности:"
    ↓
[Пользователь] → "Математик-грубиян"
    ↓
[БД] → Сохранить в personalities (is_custom=true)
    ↓
[Бот] → Приветствие от новой личности (генерируется через Claude)
```

**Реализация:**
- `modules/personalities.py` - ConversationHandler для создания
- Лимит: 5 кастомных личностей на пользователя
- Sanitization от prompt injection
- Запрет имён реальных людей/брендов

---

### Путь 2: Интеграция в групповой чат

#### 2.1 Добавление бота в группу
**Триггер:** Клик на "👥 Добавить в групповой чат"

**Флоу:**
```
[Клик] → callback_query: "add_to_group"
    ↓
[Бот] → CTA-сообщение с объяснением пользы
    + Inline-кнопка "Добавить в группу" (URL)
    ↓
[Клик] → Telegram показывает выбор группы
    ↓
[Добавление] → Бот получает событие "my_chat_member"
    ↓
[БД] → Сохранить chat_metadata
    ↓
[Бот] → Welcome-сообщение в группе
```

**CTA-сообщение:**
```
🎉 Добавь меня в свою группу!

Я смогу:
✅ Саммаризировать обсуждения
✅ Рассуживать споры
✅ Общаться в разных стилях

💡 Дай мне админ-права для полного функционала!

[Кнопка: Добавить в группу →]
```

**URL для кнопки:**
```
https://t.me/YourBot?startgroup=true
```

**Реализация:**
- `modules/onboarding.py:show_add_to_group()` - новый модуль
- `api/index.py:handle_my_chat_member()` - обработчик добавления
- `services/db_service.py:create_chat_metadata()` - сохранение

#### 2.2 Welcome-сообщение в группе
**Триггер:** Бот добавлен в группу

**Флоу:**
```
[Событие] → "my_chat_member" (status: member/administrator)
    ↓
[БД] → Создать запись в chat_metadata
    ↓
[Бот] → Welcome-сообщение с инструкциями + GIF/картинка
```

**Welcome-сообщение:**
```
👋 Привет, народ!

Я здесь, чтобы помочь! Вот что я умею:

📝 /summary - Саммаризировать обсуждения
⚖️ /rassudi - Рассудить спор
💬 /chat - Пообщаться в группе

💡 Дайте мне админ-права, чтобы я мог:
• Читать всю историю сообщений
• Удалять старые данные (безопасность)

Команды: /help
```

**Реализация:**
- `modules/onboarding.py:send_group_welcome()`
- Картинка/GIF хранится как file_id в config.py
- Проверка: если бот уже был в группе → не отправлять welcome

#### 2.3 Работа с командами в группе

##### Команда /summary (аналог /sut)
**Триггер:** Пользователь пишет `/summary` или `/summary 100`

**Флоу:**
```
[Команда] → /summary [параметр]
    ↓
[Проверки] →
    ├─ Cooldown (60 сек с последнего саммари)
    ├─ Rate limiting (10 запросов/мин на чат)
    └─ Членство пользователя в чате
    ↓
[Бот] → Меню выбора личности (inline-кнопки)
    ↓
[Выбор] → callback_query: "summary_personality:bydlan:100"
    ↓
[БД] → Получить последние N сообщений
    ↓
[Claude API] → Генерация саммари в стиле личности
    ↓
[Бот] → Ответ в чат
```

**Параметры:**
- `/summary` - последние 50 сообщений (дефолт)
- `/summary 100` - последние 100 сообщений
- `/summary 2h` - последние 2 часа
- `/summary today` - с начала дня

**Inline-меню личностей:**
```
┌──────────┬──────────┐
│ 🏭 Быдлан │ 🧙 Философ │
├──────────┼──────────┤
│ 👟 Гопник │ 💼 Олигарх │
├──────────┼──────────┤
│ 😂 Комик  │ 🔬 Учёный │
└──────────┴──────────┘
```

**Реализация:**
- `modules/summaries.py:handle_summary_command()` - обработчик
- Отличие от /sut: показываем меню личностей сразу в группе
- HMAC подпись для callback_data
- Callback содержит: personality + параметры (количество/период)

##### Команда /chat
**Триггер:** Пользователь пишет `/chat` в группе

**Флоу:**
```
[Команда] → /chat
    ↓
[Проверки] →
    ├─ Уже активна сессия для этого юзера?
    └─ Cooldown на пользователя (30 сек)
    ↓
[Бот] → Меню выбора личности
    ↓
[Выбор] → callback_query: "start_chat:bydlan"
    ↓
[БД] → Сохранить активную сессию (user_id + chat_id + personality)
    ↓
[Бот] → "Привет от [личность]! Пиши мне через reply или упоминание."
    ↓
[Пользователь] → Пишет сообщение (reply или @bot)
    ↓
[Обработка] → Только сообщения этого юзера → ответ бота
    ↓
[Завершение] → Команда /stop или таймаут 15 минут
```

**Активная сессия:**
- Хранится в `bot_data` (in-memory) или таблица `active_chat_sessions`
- Структура: `{user_id: {chat_id, personality, started_at, last_activity}}`
- Автоматическое завершение через 15 минут неактивности

**Реализация:**
- `modules/direct_chat.py:start_group_chat_session()` - новая функция
- `modules/direct_chat.py:handle_group_chat_message()` - обработчик сообщений
- Фильтр: только reply на бота или @mention
- Контекст: последние 10-15 сообщений сессии

##### Команда /rassudi
**Триггер:** `/rassudi @user1 @user2 Кто прав в споре?`

**Флоу:**
```
[Команда] → /rassudi @user1 @user2 [описание]
    ↓
[Валидация] →
    ├─ Есть ли упомянутые пользователи?
    ├─ Есть ли описание спора?
    └─ Cooldown (60 сек)
    ↓
[Бот] → Меню выбора личности
    ↓
[Выбор] → callback_query: "judge_personality:bydlan:user1:user2"
    ↓
[БД] → Получить последние 50 сообщений от @user1 и @user2
    ↓
[Claude API] → Анализ + вердикт в стиле личности
    ↓
[Бот] → Вердикт в чат
```

**Формат вердикта (пример "Быдлан"):**
```
⚖️ ВЕРДИКТ ОТ БЫДЛАНА:

Ну короче так, пацаны...

@user1 - базарит одно, но в самом деле х*ёво аргументит.
@user2 - вроде по делу, но тоже хуйню несёт.

ИТОГ: Оба клоуны, но @user2 чуть меньше.

🏭 Быдлан постановил. Базара нет.
```

**Реализация:**
- `modules/judge.py:handle_judge_command()` - уже есть
- Добавить меню личностей перед генерацией
- Парсинг @username из текста команды
- Формат callback: `judge:personality:user_ids_hash`

#### 2.4 Команды через слэш-меню
**Настройка через BotFather:**

```
/setcommands

start - Начать работу с ботом
help - Показать справку по командам
summary - Саммаризировать обсуждение
chat - Начать общение в группе
rassudi - Рассудить спор
lichnost - Выбрать/создать личность
stats - Статистика использования
```

**Реализация:**
- Команды регистрируются один раз через BotFather
- На мобильных устройствах появляется кнопка "/" с автокомплитом
- Подсказки помогают пользователям понять, что делает команда

---

### Технические детали новых фич

#### Новые модули:
```
modules/
├── direct_chat.py         # NEW: Прямое общение с ботом
├── onboarding.py          # NEW: Welcome-сообщения, добавление в группы
├── summaries.py           # UPDATE: Добавить выбор личности
├── judge.py               # UPDATE: Добавить выбор личности
└── personalities.py       # Без изменений
```

#### Новые таблицы БД:
```sql
-- Приветствия для личностей
ALTER TABLE personalities
ADD COLUMN greeting_message TEXT DEFAULT NULL;

-- Активные сессии чата (опционально, можно через bot_data)
CREATE TABLE active_chat_sessions (
  id SERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,
  chat_id BIGINT NOT NULL,
  personality VARCHAR(100) NOT NULL,
  started_at TIMESTAMP DEFAULT NOW(),
  last_activity TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, chat_id)
);

-- Индекс для быстрого поиска сессий
CREATE INDEX idx_sessions_activity ON active_chat_sessions(last_activity);
```

#### Обновление конфигурации:
```python
# config.py - новые параметры

# Direct chat settings
DIRECT_CHAT_CONTEXT_MESSAGES = 30  # Сколько сообщений в контексте
DIRECT_CHAT_SESSION_TIMEOUT = 900  # 15 минут (для групп)

# Group chat settings
GROUP_CHAT_WELCOME_IMAGE_ID = "AgACAgIAAxkBAAI..."  # file_id картинки
GROUP_ADD_URL = "https://t.me/YourBot?startgroup=true"

# Personality settings
DEFAULT_GREETING_ENABLED = True  # Генерировать приветствия через API
NO_DEFAULT_PERSONALITY = True    # Принудительный выбор личности

# Rate limiting для /chat
CHAT_SESSION_COOLDOWN = 30  # 30 сек между запусками /chat
```

---

### UX-подсказки и обработка ошибок

#### Сценарии ошибок:

**1. Пользователь пишет боту без выбора личности:**
```
[Сообщение] → "Привет"
    ↓
[Проверка] → selected_personality == NULL
    ↓
[Бот] → "Выбери личность, чтобы начать общение: /lichnost"
```

**2. Команда /summary без сообщений в чате:**
```
[Команда] → /summary
    ↓
[БД] → 0 сообщений найдено
    ↓
[Бот] → "В чате пока нет сообщений для саммари. Напишите хотя бы 10 сообщений!"
```

**3. Команда /rassudi без упоминаний:**
```
[Команда] → /rassudi Кто прав?
    ↓
[Парсинг] → Нет @username
    ↓
[Бот] → "Укажи участников спора через @username!\nПример: /rassudi @ivan @petya Кто прав?"
```

**4. Превышен лимит токенов в direct chat:**
```
[Claude API] → Error: context_length_exceeded
    ↓
[Бот] → "Контекст переполнен! Я помню только последние 3 дня.\nНачни новый диалог: /start"
```

**5. Cooldown активен:**
```
[Команда] → /summary
    ↓
[Проверка] → Последний саммари 30 сек назад
    ↓
[Бот] → "Подожди ещё 30 секунд перед следующим саммари ⏱️"
```

**Реализация:**
- `utils/validators.py:validate_command_input()` - валидация команд
- Все ошибки логируются в БД (таблица `analytics`)
- User-friendly сообщения на русском языке

---

## 🏗️ АРХИТЕКТУРА

### Принципы:
1. **Модульность** - каждая фича в отдельном файле
2. **Слабая связанность** - минимум зависимостей
3. **Типизация** - type hints везде
4. **Конфигурируемость** - всё через config.py
5. **Тестируемость** - независимые модули

### Структура проекта:

```
vkratse/
├── api/
│   └── index.py              # Webhook entry point (Vercel handler)
│
├── modules/
│   ├── direct_chat.py        # NEW: Прямое общение с ботом (1-на-1)
│   ├── onboarding.py         # NEW: Welcome, добавление в группы
│   ├── summaries.py          # /sut, /summary + callback handlers
│   ├── personalities.py      # /lichnost + создание личностей
│   ├── judge.py              # /rassudi - судейство споров
│   └── commands.py           # /start, /help, /stats
│
├── services/
│   ├── ai_service.py         # Обёртка над Claude API
│   ├── db_service.py         # Обёртка над Supabase
│   └── persistence.py        # User data persistence
│
├── models/
│   ├── message.py            # Модель сообщения
│   ├── personality.py        # Модель личности
│   ├── user.py               # Модель пользователя
│   ├── chat.py               # Модель чата
│   └── session.py            # NEW: Модель активной сессии чата
│
├── utils/
│   ├── security.py           # HMAC, sanitization
│   ├── rate_limit.py         # Rate limiting
│   ├── cooldown.py           # Cooldown чатов
│   ├── time_parser.py        # Парсинг временных диапазонов
│   └── validators.py         # Валидация данных
│
├── scripts/
│   └── cleanup_old_messages.py  # Одноразовая очистка БД
│
├── sql/
│   ├── init_tables.sql       # Создание всех таблиц
│   ├── seed_personalities.sql # Заполнение базовых личностей
│   └── migrations/
│       ├── 001_add_greetings.sql    # NEW: Добавить greeting_message
│       └── 002_chat_sessions.sql    # NEW: Таблица active_chat_sessions
│
├── config.py                 # Конфигурация (env vars)
├── requirements.txt          # Python dependencies
├── vercel.json              # Vercel configuration
├── README.md                # Основная документация
├── README_CLEANUP.md        # Инструкции по cleanup скрипту
└── CLAUDE.md                # Этот файл (для Claude Code)
```

---

## 🗄️ СХЕМА ДАННЫХ (Supabase)

### Основные таблицы

#### `messages`
```sql
CREATE TABLE messages (
  id BIGSERIAL PRIMARY KEY,
  chat_id BIGINT NOT NULL,
  user_id BIGINT,
  username TEXT,
  message_text TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  INDEX idx_chat_time (chat_id, created_at DESC)
);
```
**Автоочистка:** 3 дня (при каждом новом сообщении)

#### `personalities`
```sql
CREATE TABLE personalities (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) UNIQUE NOT NULL,
  display_name VARCHAR(100) NOT NULL,
  emoji VARCHAR(10) DEFAULT '🎭',
  system_prompt TEXT NOT NULL,
  greeting_message TEXT DEFAULT NULL,        -- NEW: Персональное приветствие
  is_custom BOOLEAN DEFAULT FALSE,
  created_by_user_id BIGINT,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### `active_chat_sessions` (NEW)
```sql
CREATE TABLE active_chat_sessions (
  id SERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,
  chat_id BIGINT NOT NULL,
  personality VARCHAR(100) NOT NULL,
  started_at TIMESTAMP DEFAULT NOW(),
  last_activity TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, chat_id),
  INDEX idx_sessions_activity (last_activity)
);
```
**Назначение:** Хранение активных сессий /chat в группах
- Автоматическое завершение через 15 минут неактивности
- Один пользователь = одна сессия на чат

#### `user_settings`
```sql
CREATE TABLE user_settings (
  user_id BIGINT PRIMARY KEY,
  username TEXT,
  selected_personality VARCHAR(100) DEFAULT 'neutral',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### `chat_metadata`
```sql
CREATE TABLE chat_metadata (
  chat_id BIGINT PRIMARY KEY,
  chat_title TEXT,
  chat_type TEXT,
  bot_added_at TIMESTAMP DEFAULT NOW(),
  last_activity TIMESTAMP DEFAULT NOW()
);
```

#### `cooldowns`
```sql
CREATE TABLE cooldowns (
  chat_id BIGINT PRIMARY KEY,
  last_summary_at TIMESTAMP,
  last_judge_at TIMESTAMP,
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### `analytics`
```sql
CREATE TABLE analytics (
  id BIGSERIAL PRIMARY KEY,
  user_id BIGINT,
  chat_id BIGINT,
  event_type VARCHAR(50),
  metadata JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 📱 КОМАНДЫ БОТА

**ВАЖНО:** Команды используют ЛАТИНСКИЕ названия (Telegram API требование)

### `/start`
Приветствие + Inline-меню выбора действия
- 💬 Общаться напрямую
- 👥 Добавить в групповой чат
- 🎭 Настроить личность

**Контекст:**
- В **ЛС**: показывает полное меню
- В **группе**: краткая справка + /help

### `/help`
Полная справка по всем командам с примерами

### `/summary [период]` (NEW)
Саммаризация чата с выбором личности
- В **группе**: показывает меню личностей → генерирует саммари
- В **ЛС**: показывает кнопки выбора чата → меню личностей

**Примеры:**
- `/summary` - последние 50 сообщений
- `/summary 100` - последние 100 сообщений
- `/summary 2h` - последние 2 часа
- `/summary today` - с начала дня

**Отличие от /sut:** Всегда запрашивает выбор личности

**Безопасность:** HMAC подпись, проверка членства, rate limiting, cooldown

### `/sut [период]`
Саммаризация чата (legacy, использует дефолтную личность пользователя)
- В **группе**: саммари текущего чата
- В **ЛС**: показывает кнопки для выбора чата

**Рекомендуется:** Использовать `/summary` для большей гибкости

### `/chat` (NEW)
Начать прямое общение с ботом в группе
- Показывает меню выбора личности
- Бот отвечает только этому пользователю (через reply/mention)
- Сессия автоматически завершается через 15 минут неактивности
- Завершить вручную: `/stop`

**Только для групп!** В ЛС не нужна (используется прямое общение)

### `/rassudi @user1 @user2 <описание>`
Рассудить спор с выбором личности судьи
- Извлекает упомянутых @username
- Показывает меню выбора личности
- Анализирует последние 50 сообщений участников
- Выдаёт вердикт в стиле выбранной личности

**Пример:**
```
/rassudi @ivan @petya Кто прав насчет выбора фреймворка?
```

### `/lichnost`
Выбор/создание личности AI
- Показывает 7 базовых личностей
- Показывает кастомные личности пользователя (до 5 штук)
- Кнопка "➕ Создать свою личность"
- Выбранная личность используется для всех взаимодействий

### `/stats`
Статистика использования
- Количество саммари
- Количество споров рассуждено
- Активные сессии чата
- Любимая личность

### `/stop` (NEW)
Завершить активную сессию /chat в группе
- Только для групп
- Освобождает сессию для нового запуска /chat

---

## 🎭 ЛИЧНОСТИ AI (7 базовых)

Все личности хранятся в БД (`personalities` таблица):

1. **🎓 Нейтральный (neutral)** - дефолт, вежливый и профессиональный
2. **🏭 Быдлан (bydlan)** - заводчанин, матюкается, про автомойку
3. **🧙 Философ (philosopher)** - мудрец, глубокие размышления
4. **👟 Гопник (gopnik)** - пацан из 2000-х, сленг, подъезд
5. **💼 Олигарх (oligarch)** - богач, яхты и миллионы
6. **😂 Стендапер (comedian)** - комик, всё в шутку
7. **🔬 Учёный (scientist)** - научный подход, исследования

**Кастомные личности:**
- Пользователи могут создавать свои (до 5 штук)
- Sanitization защищает от prompt injection
- Запрещены имена реальных людей

---

## 🔧 КОНФИГУРАЦИЯ

### Переменные окружения (.env)

```bash
# Обязательные
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-7-sonnet-20250219
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=eyJhbGci...

# Опциональные
SECRET_KEY=random_secret_for_hmac_12345

# Настройки
MESSAGE_RETENTION_DAYS=3                    # Автоудаление старых сообщений
DEFAULT_SUMMARY_HOURS=24
DEFAULT_PERSONALITY=neutral                 # Используется только для /sut
COOLDOWN_SECONDS=60
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=60
MAX_MESSAGES_PER_SUMMARY=400
DEFAULT_MESSAGE_LIMIT=50
MAX_CUSTOM_PERSONALITIES_PER_USER=5
MAX_PERSONALITY_DESCRIPTION_LENGTH=1500
MIN_PERSONALITY_DESCRIPTION_LENGTH=10
LOG_LEVEL=INFO

# NEW: Direct chat settings
DIRECT_CHAT_CONTEXT_MESSAGES=30             # Сколько сообщений в контексте
DIRECT_CHAT_SESSION_TIMEOUT=900             # 15 минут (для групп)
NO_DEFAULT_PERSONALITY=true                 # Принудительный выбор личности
DEFAULT_GREETING_ENABLED=true               # Генерировать приветствия через API

# NEW: Group chat settings
GROUP_CHAT_WELCOME_IMAGE_ID=                # file_id картинки для welcome
GROUP_ADD_URL=https://t.me/YourBot?startgroup=true
CHAT_SESSION_COOLDOWN=30                    # 30 сек между запусками /chat
```

### Важно для Vercel

**Environment Variables** должны быть установлены через Vercel Dashboard:
- Settings → Environment Variables
- Добавить все переменные для Production, Preview, Development
- После изменений сделать redeploy

---

## 🔄 АВТОУДАЛЕНИЕ СООБЩЕНИЙ

**Механизм:**
- При каждом новом сообщении в чате → автоматически удаляются сообщения старше `MESSAGE_RETENTION_DAYS` (3 дня)
- Удаление происходит только для этого чата (не глобально)
- Логируется количество удалённых сообщений

**Реализация:** `services/db_service.py:46-57`

**Cleanup скрипт:** `scripts/cleanup_old_messages.py`
- Одноразовая очистка всех старых данных
- Запускается локально (не на Vercel!)
- Инструкции в `README_CLEANUP.md`

---

## 🚀 DEPLOYMENT

### Vercel Setup

```bash
# Deploy
vercel deploy --prod

# Set environment variables через Dashboard:
# Settings → Environment Variables

# Установить webhook
curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://vkratse.vercel.app"

# Проверить webhook
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

### Deployment Checklist

**Перед deployment:**
- [ ] Все тесты проходят локально
- [ ] Команды используют Latin названия
- [ ] Application.initialize() присутствует
- [ ] Pure WSGI без Werkzeug
- [ ] Environment variables установлены в Vercel

**После deployment:**
- [ ] Логи Vercel показывают 200 OK
- [ ] Webhook установлен на production URL
- [ ] getWebhookInfo не показывает ошибок
- [ ] Команды работают: /start, /help, /sut

---

## 🎯 ТЕКУЩИЙ СТАТУС

### ✅ Что работает (Production Ready - 100%)

**Production bot:** @chto_bilo_v_chate_bot
**URL:** https://vkratse.vercel.app
**Deployment:** Vercel Serverless
**Database:** Supabase PostgreSQL
**Статус:** 🟢 Все фазы завершены

**Реализованные фичи:**

**Базовый функционал:**
- ✅ Webhook работает стабильно
- ✅ Базовые команды: /start, /help, /summary, /chat, /stop, /rassudi, /lichnost, /stats
- ✅ 7 базовых личностей с уникальными приветствиями
- ✅ Создание кастомных личностей (через ConversationHandler, до 5 на пользователя)
- ✅ Автоудаление старых сообщений (3 дня)
- ✅ Rate limiting и cooldown
- ✅ HMAC защита callback_data
- ✅ Sanitization кастомных личностей (защита от prompt injection)
- ✅ Аналитика (таблица analytics)
- ✅ User data persistence
- ✅ Cleanup скрипт для одноразовой очистки

**NEW FLOW (Фазы 1-4 - завершены):**

**Фаза 1: Инфраструктура (100%)**
- ✅ БД миграции: `sql/migrations/001_add_greetings.sql` - колонка `greeting_message`
- ✅ БД миграции: `sql/migrations/002_chat_sessions.sql` - таблица `active_chat_sessions`
- ✅ Приветствия для всех 7 базовых личностей
- ✅ `services/ai_service.py`: `generate_chat_response()` - генерация в контексте диалога
- ✅ `services/ai_service.py`: `generate_greeting()` - генерация приветствий для кастомных личностей
- ✅ `services/db_service.py`: `get_chat_history()` - получение истории сообщений

**Фаза 2: Direct Chat (100%)**
- ✅ `modules/direct_chat.py` - полностью реализован
- ✅ `show_personality_selection()` - показ меню личностей (2 колонки, базовые + кастомные)
- ✅ `handle_personality_selection()` - сохранение выбора + персональное приветствие
- ✅ `handle_direct_message()` - прямое общение 1-на-1 в ЛС с контекстом (30 сообщений)
- ✅ `handle_create_personality_callback()` - редирект на /lichnost для создания личности

**Фаза 3: Групповой функционал (100%)**
- ✅ `modules/commands.py` обновлён:
  - `/start` - inline-меню (💬 Общаться / 📊 Саммари / 👥 Добавить в группу / 🎭 Личность)
  - `handle_start_menu_callback()` - обработка всех кнопок меню
  - `show_main_menu()` - переиспользуемая функция для навигации
- ✅ `modules/direct_chat.py`:
  - `chat_command()` - команда `/chat` для запуска сессии в группах
  - `stop_command()` - команда `/stop` для завершения сессии
  - `handle_start_chat_callback()` - создание активной сессии в bot_data
  - `handle_group_chat_message()` - ответы на reply/mention с таймаутом 15 минут
- ✅ `modules/summaries.py` обновлён:
  - `/summary` - новая команда с выбором личности
  - Inline-меню личностей в группах
  - Поддержка параметров: `/summary 100`, `/summary 2h`, `/summary today`
- ✅ `modules/judge.py` обновлён:
  - Переделан через ConversationHandler
  - Меню выбора личности перед вердиктом
  - `handle_judge_personality_callback()` - обработка выбора

**Фаза 4: Onboarding (100%)**
- ✅ Welcome-сообщение при добавлении в группу (через `/start`)
- ✅ Inline-кнопки для групп: 📝 Саммари / 💬 Общаться / ⚖️ Рассудить / 🎭 Личность
- ✅ Deep-link для добавления бота: `https://t.me/{BOT_USERNAME}?startgroup=true`

### 🔮 Будущие фичи (Приоритет 3)

**В планах на следующие итерации:**
- [ ] **Монетизация** (см. раздел "💰 МОНЕТИЗАЦИЯ" ниже)
  - Интеграция Telegram Stars (нативная оплата)
  - Crypto платежи (TON, USDT)
  - Tribute.to интеграция
- [ ] Расширенная аналитика по личностям
- [ ] Экспорт саммари в файлы (PDF, TXT, MD)
- [ ] Scheduled summaries (крон-задачи через внешний сервис)
- [ ] Premium функции (приоритетная обработка, расширенный контекст)
- [ ] Голосовые сообщения (voice → text → ответ)
- [ ] Мультиязычность (EN, RU, etc.)
- [ ] Интеграция с другими мессенджерами (Discord, Slack)

---

## 💰 МОНЕТИЗАЦИЯ

### Стратегия монетизации

**Цель:** Создать устойчивую модель монетизации с низким порогом входа и гибкими вариантами оплаты.

**Принципы:**
1. **Freemium модель** - базовые функции бесплатны, расширенные — платные
2. **Низкий порог входа** - минимальная цена ~$1-3/месяц
3. **Гибкая оплата** - Telegram Stars, криптовалюта, Tribute
4. **Прозрачность** - чёткие лимиты и преимущества

---

### 📊 Pricing Tiers (Тарифные планы)

#### 🆓 Free Tier (Бесплатный)
**Доступно без ограничений:**
- ✅ Прямое общение с ботом (1-на-1) - до 50 сообщений/день
- ✅ 1 групповой чат
- ✅ Саммаризация: до 5 саммари/день
- ✅ Судейство: до 3 вердиктов/день
- ✅ 7 базовых личностей
- ✅ 1 кастомная личность
- ✅ Контекст: 30 сообщений
- ✅ История: 3 дня

**Лимиты:**
- 🔒 Cooldown: 60 секунд между саммари
- 🔒 Максимум 50 сообщений за саммари
- 🔒 Стандартная скорость ответа

---

#### ⭐ Pro Tier ($2.99/месяц или 300 Telegram Stars)
**Все из Free +:**
- ✨ Прямое общение: до 500 сообщений/день
- ✨ Неограниченное количество групповых чатов
- ✨ Саммаризация: до 50 саммари/день
- ✨ Судейство: до 20 вердиктов/день
- ✨ Все базовые личности
- ✨ До 5 кастомных личностей
- ✨ Расширенный контекст: 50 сообщений
- ✨ История: 7 дней
- ✨ Cooldown: 30 секунд
- ✨ Максимум 200 сообщений за саммари
- ✨ Приоритетная обработка запросов
- ✨ Экспорт саммари в файлы (PDF, TXT, MD)

**Цена:** $2.99/месяц или 300 Telegram Stars (~$3)

---

#### 💎 Premium Tier ($9.99/месяц или 1000 Telegram Stars)
**Все из Pro +:**
- 💎 Прямое общение: неограниченно
- 💎 Саммаризация: неограниченно
- 💎 Судейство: неограниченно
- 💎 До 15 кастомных личностей
- 💎 Максимальный контекст: 100 сообщений
- 💎 История: 30 дней
- 💎 Без cooldown
- 💎 Максимум 1000 сообщений за саммари
- 💎 Высший приоритет обработки
- 💎 Scheduled summaries (крон-задачи)
- 💎 Голосовые сообщения (voice → text → ответ)
- 💎 Ранний доступ к новым фичам
- 💎 Персональная поддержка

**Цена:** $9.99/месяц или 1000 Telegram Stars (~$10)

---

### 💳 Способы оплаты

#### 1. Telegram Stars (Приоритет 1) ⭐
**Преимущества:**
- ✅ Нативная интеграция в Telegram
- ✅ Простой API для оплаты
- ✅ Автоматическая конвертация валют
- ✅ Безопасные транзакции
- ✅ Не требует дополнительных сервисов

**Реализация:**
- Telegram Bot Payments API
- `python-telegram-bot` имеет встроенную поддержку
- Минимальная комиссия (~5%)

**Цены в Stars:**
- Pro: 300 Stars (~$3)
- Premium: 1000 Stars (~$10)

**Документация:** https://core.telegram.org/bots/payments

---

#### 2. Crypto платежи (TON, USDT) (Приоритет 2) 💎
**Преимущества:**
- ✅ Низкие комиссии
- ✅ Децентрализация
- ✅ Глобальный доступ
- ✅ TON — нативная криптовалюта Telegram

**Поддерживаемые монеты:**
- **TON** (The Open Network) - приоритет
- **USDT** (Tether) - стабильная монета
- **BTC** (Bitcoin) - опционально

**Сервисы для интеграции:**
- [@CryptoBot](https://t.me/CryptoBot) - Telegram-нативный crypto wallet
- [TON Connect](https://ton.org/dev/payments) - официальный SDK для TON
- [Cryptomus](https://cryptomus.com/) - мульти-крипто платежи

**Реализация:**
- API интеграция через webhook
- Проверка транзакций через blockchain explorer
- Автоматическая активация подписки

**Цены в Crypto:**
- Pro: ~3 USDT или 30 TON
- Premium: ~10 USDT или 100 TON

---

#### 3. Tribute.to (Приоритет 3) 🎁
**Преимущества:**
- ✅ Поддержка разовых донатов
- ✅ Красивая страница с благодарностями
- ✅ Возможность публичных благодарностей

**Модель:**
- Не подписка, а **разовые донаты** или **донат-тир**
- Пользователи донатят произвольную сумму → получают бонусы

**Tribute тиры:**
- **$5 донат** → Pro на 2 месяца
- **$10 донат** → Pro на 5 месяцев
- **$25 донат** → Premium на 3 месяца
- **$50 донат** → Premium на 7 месяцев + особая кастомная личность

**Реализация:**
- Ручная активация (пока нет API у Tribute)
- Пользователь донатит → присылает скриншот/подтверждение → админ активирует
- Альтернатива: webhook через Zapier/Make

**Страница Tribute:** https://tribute.to/your_bot_page

---

### 🛠️ Техническая реализация

#### Архитектура подписок

**Новая таблица БД: `subscriptions`**
```sql
CREATE TABLE subscriptions (
  id SERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL UNIQUE,
  tier VARCHAR(20) NOT NULL,  -- 'free', 'pro', 'premium'
  started_at TIMESTAMP DEFAULT NOW(),
  expires_at TIMESTAMP,  -- NULL для free tier
  payment_method VARCHAR(50),  -- 'stars', 'ton', 'usdt', 'tribute'
  transaction_id VARCHAR(255),  -- Ссылка на транзакцию
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_subscriptions_user ON subscriptions(user_id);
CREATE INDEX idx_subscriptions_expiry ON subscriptions(expires_at);
```

**Новая таблица: `usage_limits`**
```sql
CREATE TABLE usage_limits (
  id SERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,
  date DATE DEFAULT CURRENT_DATE,
  messages_count INT DEFAULT 0,
  summaries_count INT DEFAULT 0,
  judge_count INT DEFAULT 0,
  UNIQUE(user_id, date)
);

CREATE INDEX idx_usage_date ON usage_limits(user_id, date);
```

---

#### Модули для реализации

**1. `modules/payments.py` - обработка платежей**
```python
async def handle_payment_stars(update, context):
    """Handle Telegram Stars payment"""
    # Process payment callback
    # Update subscription in DB
    # Send confirmation

async def handle_payment_crypto(update, context):
    """Handle crypto payment verification"""
    # Check transaction on blockchain
    # Update subscription in DB

async def handle_payment_tribute(update, context):
    """Handle Tribute.to donation (manual)"""
    # Show instructions for Tribute
    # Admin verification flow
```

**2. `modules/subscription.py` - управление подписками**
```python
async def check_user_tier(user_id: int) -> str:
    """Get user's subscription tier"""
    # Check DB, return 'free', 'pro', or 'premium'

async def check_usage_limit(user_id: int, action: str) -> bool:
    """Check if user exceeded daily limits"""
    # Check usage_limits table
    # Compare with tier limits

async def upgrade_subscription(user_id: int, tier: str, duration_days: int):
    """Upgrade user's subscription"""
    # Update subscriptions table
    # Send confirmation message
```

**3. Middleware для проверки лимитов**
```python
async def check_limits_middleware(update, context):
    """Middleware to check user limits before processing"""
    user_id = update.effective_user.id
    action = determine_action(update)  # 'message', 'summary', 'judge'

    tier = await check_user_tier(user_id)
    can_proceed = await check_usage_limit(user_id, action)

    if not can_proceed:
        await show_upgrade_message(update)
        return False

    return True
```

---

#### Команды для пользователей

**`/premium` - показать доступные планы**
```
💎 Premium планы

🆓 FREE (текущий план)
• 50 сообщений/день
• 5 саммари/день
• 1 группа

⭐ PRO - $2.99/мес
• 500 сообщений/день
• 50 саммари/день
• Неограниченные группы
• Экспорт в файлы

💎 PREMIUM - $9.99/мес
• Без лимитов
• 15 кастомных личностей
• Голосовые сообщения
• Приоритет

[Кнопки: Купить Pro | Купить Premium | Сравнить планы]
```

**`/mystatus` - показать текущий статус**
```
📊 Твой статус

Тариф: ⭐ Pro
Активен до: 2025-12-17
Осталось: 30 дней

Использовано сегодня:
💬 Сообщения: 125/500
📝 Саммари: 12/50
⚖️ Судейство: 5/20

[Кнопка: Продлить подписку]
```

**`/cancel` - отменить подписку**
```
❌ Отмена подписки

Ты уверен?

Твой Pro план активен до 2025-12-17.
После этой даты вернёшься на Free план.

[Кнопки: Да, отменить | Нет, оставить]
```

---

#### Интеграция в существующие команды

**Изменения в обработчиках:**

1. **`handle_direct_message()` - проверка лимита сообщений**
```python
tier = await check_user_tier(user_id)
if tier == 'free' and daily_messages >= 50:
    await show_upgrade_message(update, "Лимит сообщений исчерпан")
    return
```

2. **`summary_command()` - проверка лимита саммари**
```python
tier = await check_user_tier(user_id)
if tier == 'free' and daily_summaries >= 5:
    await show_upgrade_message(update, "Лимит саммари исчерпан")
    return
```

3. **`judge_command()` - проверка лимита судейства**
```python
tier = await check_user_tier(user_id)
if tier == 'free' and daily_judges >= 3:
    await show_upgrade_message(update, "Лимит судейства исчерпан")
    return
```

---

### 📈 Метрики успеха

**KPI для отслеживания:**
- Conversion rate (free → pro): целевой **3-5%**
- Retention rate (через месяц): целевой **70%**
- ARPU (Average Revenue Per User): целевой **$1-2**
- Churn rate: целевой **<20%/месяц**

**A/B тесты:**
- Разные цены ($1.99 vs $2.99 vs $4.99)
- Разные лимиты free tier (30 vs 50 vs 100 сообщений)
- Разные тексты upgrade сообщений

---

### 🚀 План внедрения монетизации

**Фаза 1: Подготовка (1-2 недели)**
- [ ] Создать таблицы `subscriptions` и `usage_limits`
- [ ] Реализовать `modules/subscription.py` (проверка тиров, лимитов)
- [ ] Добавить middleware для проверки лимитов
- [ ] Реализовать команды `/premium`, `/mystatus`
- [ ] Тестирование на staging

**Фаза 2: Telegram Stars (1 неделя)**
- [ ] Реализовать `modules/payments.py` - Telegram Stars
- [ ] Настроить Telegram Bot Payments API
- [ ] Интегрировать webhook для подтверждения платежей
- [ ] Тестирование платежей (test mode)
- [ ] Launch на production

**Фаза 3: Crypto (2 недели)**
- [ ] Интеграция с @CryptoBot API
- [ ] Реализовать TON Connect
- [ ] Добавить USDT через Cryptomus
- [ ] Тестирование транзакций
- [ ] Launch crypto платежей

**Фаза 4: Tribute (1 неделя)**
- [ ] Создать страницу на Tribute.to
- [ ] Реализовать ручной процесс активации
- [ ] Добавить команду `/donate`
- [ ] Настроить webhook (если возможно)
- [ ] Публичное объявление

**Фаза 5: Оптимизация (постоянно)**
- [ ] Анализ метрик и конверсий
- [ ] A/B тесты ценообразования
- [ ] Улучшение upgrade messaging
- [ ] Добавление новых premium фич

---

## 🧪 ТЕСТИРОВАНИЕ

### Ручное тестирование

#### Тестирование НОВОГО флоу (Приоритет 1):

**1. Первый контакт в ЛС:**
- [ ] /start показывает приветствие + inline-меню (💬 Общаться / 👥 Добавить / 🎭 Личность)
- [ ] Клик "💬 Общаться напрямую" → показывает меню личностей
- [ ] Клик на личность → персональное приветствие
- [ ] Написать сообщение → бот отвечает в стиле личности
- [ ] Написать ещё 10 сообщений → контекст сохраняется
- [ ] /lichnost → меню смены личности
- [ ] Сменить личность → история НЕ очищается, стиль меняется
- [ ] Клик "➕ Создать личность" → ConversationHandler работает
- [ ] Создать кастомную → приветствие генерируется через API

**2. Добавление в группу:**
- [ ] Клик "👥 Добавить в групповой чат" → CTA-сообщение + кнопка
- [ ] Клик кнопки → Telegram предлагает выбрать группу
- [ ] Добавить бота → welcome-сообщение в группе
- [ ] Welcome содержит команды: /summary, /chat, /rassudi

**3. Команды в группе:**
- [ ] /summary в группе → показывает меню личностей
- [ ] Клик на личность → саммари в стиле личности
- [ ] /summary 100 → саммари 100 последних сообщений
- [ ] /summary 2h → саммари за 2 часа
- [ ] /chat → показывает меню личностей
- [ ] Клик на личность → "Пиши мне через reply/mention"
- [ ] Reply на сообщение бота → бот отвечает
- [ ] @bot_mention текст → бот отвечает
- [ ] Другой пользователь пишет → бот НЕ отвечает (не его сессия)
- [ ] Ждём 15 минут → сессия автозавершается
- [ ] /stop → сессия завершается вручную
- [ ] /rassudi @user1 @user2 спор → показывает меню личностей
- [ ] Клик на личность → вердикт в стиле личности

**4. Обработка ошибок:**
- [ ] Пользователь пишет без выбора личности → подсказка "/lichnost"
- [ ] /summary в пустом чате → "Напишите хотя бы 10 сообщений"
- [ ] /rassudi без @username → подсказка с примером
- [ ] /chat когда сессия активна → "Сессия уже запущена, /stop для завершения"
- [ ] Cooldown активен → "Подожди X секунд"

#### Тестирование LEGACY функций:

5. Создать тестовую группу с друзьями
6. Добавить бота
7. Написать 50+ сообщений
8. Протестировать все команды:
   - [ ] /start показывает приветствие
   - [ ] /help показывает справку
   - [ ] /sut в группе работает
   - [ ] /sut в ЛС показывает кнопки чатов
   - [ ] Клик по кнопке → саммари в ЛС
   - [ ] /rassudi выдаёт вердикт
   - [ ] /lichnost показывает личности
   - [ ] Создание кастомной личности работает
   - [ ] /stats показывает статистику
9. Проверить автоудаление (подождать 3 дня)
10. Попробовать заспамить (cooldown должен сработать)
11. Удалить бота (данные должны очиститься)

### Безопасность

- [ ] HMAC подписи проверяются
- [ ] Нельзя получить саммари чата где не состоишь
- [ ] Rate limiting блокирует спам
- [ ] Cooldown блокирует частые запросы в чате
- [ ] Sanitization блокирует prompt injection
- [ ] Нельзя создать личность с запрещёнными словами

---

## 💡 КЛЮЧЕВЫЕ МОМЕНТЫ ДЛЯ CLAUDE

### При работе с кодом:

1. **Модульность** - каждая фича в отдельном модуле
2. **Типизация** - все функции с type hints
3. **Комментарии** - docstrings везде
4. **Конфигурация** - всё через config.py
5. **Безопасность** - всегда sanitize пользовательский ввод
6. **Проверки** - всегда проверять членство перед доступом к данным
7. **Логирование** - логировать важные события
8. **Обработка ошибок** - try/except для внешних API

### Важные требования:

- Команды только на LATIN (Telegram API requirement)
- Pure WSGI (без Werkzeug для Vercel)
- Application.initialize() перед использованием
- Graceful degradation для импортов
- HMAC для callback_data безопасности
- Rate limiting и cooldown для защиты от абуза

### Специфика НОВОГО флоу:

1. **Нет дефолтной личности** - пользователь ВСЕГДА выбирает явно
2. **Inline-меню везде** - все действия через callback_query с кнопками
3. **Контекст истории** - хранить последние N сообщений для связности диалога
4. **Персонализация** - каждая личность имеет уникальное приветствие
5. **Сессии чата** - для /chat в группах использовать таблицу или bot_data
6. **User-friendly ошибки** - всегда давать подсказки, что делать дальше
7. **HMAC для всего** - любой callback_data должен быть подписан
8. **Генерация через API** - приветствия для кастомных личностей генерируются динамически

### Порядок реализации нового флоу:

**Фаза 1: Инфраструктура (БД + сервисы)**
1. SQL миграции (добавить greeting_message, active_chat_sessions)
2. Обновить `db_service.py` (chat history, sessions)
3. Обновить `ai_service.py` (chat response, greetings)

**Фаза 2: Основной функционал (direct chat)**
1. Создать `direct_chat.py` (personality selection, message handling)
2. Обновить `/start` в `commands.py` (inline-меню)
3. Тестирование 1-на-1 общения в ЛС

**Фаза 3: Групповой функционал**
1. Создать `onboarding.py` (welcome messages)
2. Добавить `/summary` в `summaries.py`
3. Добавить `/chat` в `direct_chat.py`
4. Обновить `/rassudi` в `judge.py` (меню личностей)
5. Добавить `/stop` в `commands.py`

**Фаза 4: Полировка**
1. Заполнить приветствия для всех 7 личностей
2. Тестирование всех флоу
3. Обработка edge cases
4. Deployment

### Примеры callback_data структур:

```python
# Выбор личности для прямого чата
callback_data = "select_personality:bydlan"

# Выбор личности для саммари
callback_data = "summary_personality:bydlan:100"  # bydlan, 100 messages

# Выбор личности для судейства
callback_data = "judge_personality:gopnik:user_hash"

# Запуск сессии чата в группе
callback_data = "start_chat:philosopher"

# Добавление в группу (URL button)
url = "https://t.me/YourBot?startgroup=true"
```

### Обработка контекста в direct chat:

```python
# Псевдокод для handle_message()
def handle_message(update, context):
    user_id = update.effective_user.id
    chat_id = update.effective_chat.id
    message = update.message.text

    # 1. Проверить выбрана ли личность
    personality = db.get_user_personality(user_id)
    if not personality:
        return "Выбери личность: /lichnost"

    # 2. Сохранить сообщение юзера
    db.save_message(chat_id, user_id, message)

    # 3. Получить контекст (последние 30 сообщений)
    history = db.get_chat_history(chat_id, user_id, limit=30)

    # 4. Генерация ответа
    response = ai_service.generate_chat_response(
        personality=personality,
        history=history,
        message=message
    )

    # 5. Отправить и сохранить
    update.message.reply_text(response)
    db.save_message(chat_id, bot_id, response)
```

---

## 📚 ДОКУМЕНТАЦИЯ

- **README.md** - основная документация проекта
- **README_CLEANUP.md** - инструкции по cleanup скрипту
- **CLAUDE.md** - этот файл, для Claude Code
- **sql/*.sql** - SQL скрипты для БД

---

## 🐛 ИЗВЕСТНЫЕ ОСОБЕННОСТИ

### Vercel Serverless Limitations

1. **No background tasks** - нельзя запускать фоновые задачи
2. **Stateless** - каждый запрос независим
3. **Timeout** - максимум 10 секунд на запрос
4. **No cron jobs** - для scheduled tasks нужен внешний сервис

### Решения:

- **Автоудаление** - при каждом новом сообщении
- **Cleanup скрипт** - запускается локально
- **User data** - хранится в bot_data через persistence
- **Cooldown** - in-memory, переживает между запросами благодаря bot_data

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- [python-telegram-bot docs](https://docs.python-telegram-bot.org/)
- [Anthropic Claude API](https://docs.anthropic.com/)
- [Supabase docs](https://supabase.com/docs)
- [Vercel Python runtime](https://vercel.com/docs/runtimes#official-runtimes/python)

---

**Последнее обновление:** 2025-11-17
**Версия бота:** Production v2.0 (NEW FLOW завершён - 100%)
**Статус:** 🟢 Все фазы реализованы, готов к монетизации

---

## 📝 CHANGELOG

### 2025-11-17: Завершение всех фаз разработки (v2.0) 🎉
**ВСЕ ФАЗЫ ЗАВЕРШЕНЫ - PRODUCTION READY!**

**Реализовано:**
- ✅ **Фаза 1: Инфраструктура (100%)**
  - БД миграции: `greeting_message`, `active_chat_sessions`
  - Обновлены сервисы: `ai_service.py`, `db_service.py`
  - Приветствия для всех 7 базовых личностей

- ✅ **Фаза 2: Direct Chat (100%)**
  - Модуль `direct_chat.py` полностью реализован
  - Прямое общение 1-на-1 с контекстом (30 сообщений)
  - Выбор личности через inline-меню
  - Персональные приветствия

- ✅ **Фаза 3: Групповой функционал (100%)**
  - Команды `/chat`, `/stop` для групповых сессий
  - Обновлены `/summary`, `/rassudi` с выбором личности
  - Inline-меню во всех командах
  - Таймаут сессий 15 минут

- ✅ **Фаза 4: Onboarding (100%)**
  - Welcome-сообщение через `/start` в группах
  - Deep-link для добавления бота
  - Inline-кнопки для всех функций

**Добавлено:**
- 📋 Детальный раздел "💰 МОНЕТИЗАЦИЯ"
  - 3 тарифных плана: Free, Pro ($2.99), Premium ($9.99)
  - 3 способа оплаты: Telegram Stars, Crypto (TON/USDT), Tribute
  - Архитектура подписок: таблицы `subscriptions`, `usage_limits`
  - План внедрения монетизации (5 фаз)
  - Метрики успеха и KPI

**Готово к:**
- 🚀 Production deployment v2.0
- 💰 Внедрение монетизации (следующая итерация)
- 📊 Сбор метрик и аналитики

### 2025-11-12: Декомпозиция нового флоу
- ✅ Добавлен раздел **Customer Journey Map (CJM)**
- ✅ Детализированы 2 основных пути: личный чат + групповой чат
- ✅ Описаны новые команды: `/summary`, `/chat`, `/stop`
- ✅ Спроектирована архитектура новых модулей: `direct_chat.py`, `onboarding.py`
- ✅ Добавлены новые таблицы БД: `active_chat_sessions`, `greeting_message`
- ✅ Описаны все UX-сценарии и обработка ошибок
- ✅ Создан TODO-лист для поэтапной реализации (4 фазы)
- ✅ Добавлены примеры кода и callback_data структур

### 2025-11-14: Предыдущее обновление
- Обновлена документация по cleanup скрипту
- Сокращён retention до 2 дней
