# 🧪 Инструкция по тестированию Шага 6: Безлимитные личности для Pro

**Дата:** 2025-11-17
**Статус:** ✅ Код готов к тестированию

---

## 📋 Что было реализовано

### Ключевая фича Pro-подписки:
- **Pro-пользователи:** Безлимитное использование всех личностей
- **Free-пользователи:** Лимит 5 использований каждой личности в день (кроме Нейтральной)
- **Нейтральная личность:** Всегда доступна без лимитов

### Где работает:
1. ✅ `/summary` - саммаризация с выбором личности
2. ✅ Прямой чат в ЛС с ботом
3. ✅ `/rassudi` - судейство споров

### Дополнительно:
- ✅ Команда `/mystatus` теперь показывает использование личностей для Free-пользователей

---

## 🔧 Подготовка к тестированию

### Шаг 1: Убедитесь что БД готова

Проверьте, что таблица `personality_usage` создана:
```sql
-- Запустите в Supabase SQL Editor
SELECT * FROM personality_usage LIMIT 1;
```

Если таблицы нет → см. `sql/migrations/003_personality_usage.sql`

### Шаг 2: Узнайте свой Telegram ID

Напишите боту `/start` и посмотрите в логи Vercel - там будет ваш `user_id`.

Или используйте бота [@userinfobot](https://t.me/userinfobot)

---

## 🧪 Сценарий 1: Free-пользователь (лимиты работают)

### 1.1 Подготовка
```sql
-- Проверьте что у вас НЕТ активной подписки
SELECT * FROM subscriptions WHERE user_id = <ваш_user_id>;

-- Если есть активная Pro → деактивируйте
UPDATE subscriptions
SET is_active = false
WHERE user_id = <ваш_user_id>;
```

### 1.2 Тест лимита в саммари

1. Откройте групповой чат с ботом (или создайте)
2. Напишите несколько сообщений (10-20 штук)
3. Используйте `/summary` и выберите личность **Быдлан** (🏭)
4. Повторите эту операцию **5 раз подряд** (всегда выбирайте Быдлана)

**Ожидаемый результат:**
- ✅ Первые 5 раз саммари генерируется успешно
- ✅ На **6-й раз** появляется сообщение:
  ```
  ⚠️ Лимит использования личности 'Быдлан' исчерпан (5/5).

  💎 Pro-подписка дает безлимитное использование всех личностей!
  Узнать больше: /premium
  ```

### 1.3 Тест лимита в прямом чате

1. Откройте личный чат с ботом
2. Выберите личность через `/lichnost` → **Философ** (🧙)
3. Отправьте боту **5 сообщений** подряд
4. Отправьте **6-е сообщение**

**Ожидаемый результат:**
- ✅ Первые 5 сообщений обрабатываются
- ✅ На 6-е сообщение приходит уведомление о лимите

### 1.4 Проверка /mystatus

Отправьте команду `/mystatus`

**Ожидаемый результат:**
```
📊 Твой статус

Тариф: 🆓 Free

Использовано сегодня:
💬 Сообщения: 5/30
📝 Саммари (ЛС): 0/3
📊 Саммари (группы): 5/3
⚖️ Судейство: 0/2

🎭 Личности: 5 использований/день (кроме Нейтральной)

Использовано сегодня:
  • Быдлан: 5/15 (📝5 💬0 ⚖️0)
  • Философ: 5/15 (📝0 💬5 ⚖️0)

💡 Обновись до Pro: /premium
```

---

## 💎 Сценарий 2: Pro-пользователь (без лимитов)

### 2.1 Активация Pro

**Вариант А: Через команду /grantpro (если вы админ)**
```bash
/grantpro <ваш_user_id> 30
```

**Вариант Б: Прямо в БД**
```sql
-- Активировать Pro на 30 дней
INSERT INTO subscriptions (user_id, tier, expires_at, is_active, payment_method)
VALUES (
  <ваш_user_id>,
  'pro',
  NOW() + INTERVAL '30 days',
  true,
  'manual'
)
ON CONFLICT (user_id) DO UPDATE
SET
  tier = 'pro',
  expires_at = NOW() + INTERVAL '30 days',
  is_active = true,
  updated_at = NOW();
```

### 2.2 Тест безлимитного использования

1. Используйте `/summary` с личностью **Быдлан** → **10 раз подряд**
2. В прямом чате отправьте **10 сообщений** подряд (с личностью Философ)
3. Используйте `/rassudi @user1 @user2 спор` с личностью **Гопник** → 5 раз

**Ожидаемый результат:**
- ✅ ВСЕ операции проходят успешно
- ✅ НЕТ сообщений о лимитах
- ✅ Личности используются без ограничений

### 2.3 Проверка /mystatus

Отправьте команду `/mystatus`

**Ожидаемый результат:**
```
📊 Твой статус

Тариф: 💎 Pro
Активен до: 2025-12-17
Осталось: 30 дней

Использовано сегодня:
💬 Сообщения: 10/500
📝 Саммари (ЛС): 0/10
📊 Саммари (группы): 10/20
⚖️ Судейство: 5/20

✨ Личности: Безлимитно ♾️
```

---

## ✅ Сценарий 3: Нейтральная личность (всегда доступна)

### 3.1 Тест для Free-пользователя

1. Убедитесь что вы Free (см. Сценарий 1.1)
2. Выберите личность **Нейтральный** в `/lichnost`
3. Отправьте боту **20 сообщений** подряд
4. Используйте `/summary` с Нейтральной личностью **10 раз**

**Ожидаемый результат:**
- ✅ Все операции проходят успешно
- ✅ Нейтральная личность не имеет лимитов даже на Free

---

## 🔄 Сценарий 4: Переход Pro → Free (даунгрейд)

### 4.1 Деактивация Pro

```sql
-- Установите подписку как истекшую
UPDATE subscriptions
SET expires_at = NOW() - INTERVAL '1 day'
WHERE user_id = <ваш_user_id>;
```

### 4.2 Проверка автоматического даунгрейда

1. Отправьте любую команду боту (например, `/mystatus`)
2. Бот должен автоматически перевести вас на Free

**Ожидаемый результат:**
- ✅ `/mystatus` показывает "🆓 Free"
- ✅ Лимиты личностей снова активны

### 4.3 Проверка лимитов после даунгрейда

1. Используйте Быдлана для саммари **5 раз**
2. На 6-й раз должен появиться лимит

**Ожидаемый результат:**
- ✅ Лимиты работают снова

---

## 🛠️ Ручное управление подписками (для владельца бота)

### Активировать Pro для пользователя:
```sql
INSERT INTO subscriptions (user_id, tier, expires_at, is_active, payment_method)
VALUES (<user_id>, 'pro', NOW() + INTERVAL '30 days', true, 'manual')
ON CONFLICT (user_id) DO UPDATE
SET tier = 'pro', expires_at = NOW() + INTERVAL '30 days', is_active = true, updated_at = NOW();
```

### Деактивировать Pro:
```sql
UPDATE subscriptions
SET is_active = false, updated_at = NOW()
WHERE user_id = <user_id>;
```

### Посмотреть все активные подписки:
```sql
SELECT user_id, tier, expires_at, is_active, payment_method
FROM subscriptions
WHERE is_active = true;
```

### Посмотреть использование личностей пользователем:
```sql
SELECT personality_name, summary_count, chat_count, judge_count
FROM personality_usage
WHERE user_id = <user_id>
  AND date = CURRENT_DATE
ORDER BY (summary_count + chat_count + judge_count) DESC;
```

### Сбросить лимиты пользователя на сегодня:
```sql
-- Удалить все записи использования за сегодня
DELETE FROM personality_usage
WHERE user_id = <user_id>
  AND date = CURRENT_DATE;

DELETE FROM usage_limits
WHERE user_id = <user_id>
  AND date = CURRENT_DATE;
```

---

## 🐛 Troubleshooting

### Проблема: Лимиты не срабатывают для Free

**Решение:**
1. Проверьте что подписка неактивна:
   ```sql
   SELECT * FROM subscriptions WHERE user_id = <user_id>;
   ```
2. Проверьте логи Vercel - ищите "check_personality_limit"
3. Убедитесь что функция вызывается в правильном месте

### Проблема: Pro-пользователь видит лимиты

**Решение:**
1. Проверьте что подписка активна и не истекла:
   ```sql
   SELECT tier, is_active, expires_at FROM subscriptions WHERE user_id = <user_id>;
   ```
2. Проверьте логи - функция должна возвращать `'can_proceed': True, 'limit': -1`

### Проблема: /mystatus не показывает личности

**Решение:**
1. Убедитесь что вы использовали личности сегодня
2. Проверьте таблицу:
   ```sql
   SELECT * FROM personality_usage
   WHERE user_id = <user_id> AND date = CURRENT_DATE;
   ```

---

## 📊 Чеклист успешного тестирования

- [ ] Free: лимит 5 использований личности работает (саммари)
- [ ] Free: лимит 5 использований личности работает (прямой чат)
- [ ] Free: лимит 5 использований личности работает (судейство)
- [ ] Free: Нейтральная личность всегда доступна
- [ ] Free: `/mystatus` показывает топ-3 личности с детализацией
- [ ] Pro: безлимитное использование всех личностей
- [ ] Pro: `/mystatus` показывает "Личности: Безлимитно ♾️"
- [ ] Даунгрейд Pro→Free: лимиты снова активируются
- [ ] Лимиты сбрасываются на следующий день

---

## 🎉 Готово!

Если все тесты прошли успешно - **Шаг 6 завершен на 100%!**

Следующий шаг: **Шаг 7 - Проверка членства в группе** (бонусы за вступление в группу проекта)

---

**Последнее обновление:** 2025-11-17
