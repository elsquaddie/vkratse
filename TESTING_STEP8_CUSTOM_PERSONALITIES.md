# 🧪 Тестирование Шага 8: Кастомные личности

**Дата:** 2025-11-17
**Что тестируем:** Систему лимитов на создание кастомных личностей

---

## 📋 Что нужно для тестирования

1. **Доступ к боту** - твой Telegram аккаунт
2. **Доступ к БД Supabase** - для проверки данных и ручного изменения статуса
3. **Тестовый друг** (опционально) - для проверки сценария "Pro, в группе"
4. **Telegram группа проекта** - ID группы должен быть в `PROJECT_TELEGRAM_GROUP_ID`

---

## 🎯 Сценарии тестирования

### Тест 1: Free пользователь, НЕ в группе ❌

**Цель:** Проверить что Free-пользователь без группы НЕ может создать личность

**Шаги:**
1. Убедись что у тебя Free-тариф:
   ```sql
   -- В Supabase выполни:
   DELETE FROM subscriptions WHERE user_id = ТВОЙ_USER_ID;
   ```

2. Убедись что ты НЕ в группе проекта (или выйди из неё)

3. Открой бота, отправь `/lichnost`

4. Нажми кнопку **"➕ Создать личность"**

**Ожидаемый результат:**
```
⚠️ Лимит кастомных личностей: 0/0

Чтобы создать свою личность:
• Вступи в группу проекта для получения 1 бонусного слота
или
• Обновись до Pro: /premium (3 слота)
```

**Проверка:**
- ✅ Процесс создания НЕ начался
- ✅ Показано сообщение с инструкциями
- ✅ Упомянуты оба варианта: группа и Pro

---

### Тест 2: Free пользователь, вступил в группу ✅

**Цель:** Проверить что Free-пользователь В группе может создать 1 личность

**Шаги:**
1. Убедись что у тебя Free-тариф (см. Тест 1, шаг 1)

2. Вступи в группу проекта (та группа, ID которой в `PROJECT_TELEGRAM_GROUP_ID`)

3. Подожди 1-2 минуты (бот должен обработать событие вступления)

4. Проверь в БД что кеш обновился:
   ```sql
   SELECT * FROM group_membership_cache WHERE user_id = ТВОЙ_USER_ID;
   -- Должно быть: is_member = true
   ```

5. Отправь боту `/lichnost`

6. Нажми **"➕ Создать личность"**

**Ожидаемый результат:**
```
🎭 Создание своей личности

Шаг 1 из 2

Как назовём личность?

💡 Примеры:
• Пират
• Мастер по ноготочкам
• Космический ковбой

Напиши название или /cancel для отмены.
```

**Проверка:**
- ✅ Процесс создания НАЧАЛСЯ
- ✅ Бот просит ввести название

**Продолжение:**
7. Введи название, например: `Космонавт`

8. Введи описание, например:
   ```
   Говорит как космонавт, использует термины про орбиту, невесомость и космические полёты. Вдохновляющий и мечтательный.
   ```

9. Дождись подтверждения:
   ```
   ✅ Личность "Космонавт" 🎭 создана и выбрана!
   ```

10. Проверь в БД:
    ```sql
    SELECT name, display_name, is_custom, is_group_bonus, created_by_user_id
    FROM personalities
    WHERE name = 'космонавт';
    ```

**Ожидаемый результат в БД:**
- `is_custom` = `true`
- `is_group_bonus` = `true` (важно!)
- `created_by_user_id` = ТВОЙ_USER_ID

**Проверка:**
- ✅ Личность создана
- ✅ Поле `is_group_bonus = true` (это бонус за группу)

---

### Тест 3: Free пользователь пытается создать 2-ю личность ❌

**Цель:** Проверить что Free-пользователь в группе может создать только 1 личность

**Предусловие:** Выполнен Тест 2, создана 1 личность

**Шаги:**
1. Отправь `/lichnost`

2. Нажми **"➕ Создать личность"**

**Ожидаемый результат:**
```
⚠️ Лимит кастомных личностей: 1/1

Обновись до Pro для создания еще 3 личностей: /premium
```

**Проверка:**
- ✅ Процесс создания НЕ начался
- ✅ Показано сообщение о лимите
- ✅ Предложено купить Pro

---

### Тест 4: Активировать Pro, НЕ в группе 💎

**Цель:** Проверить что Pro-пользователь может создать 3 личности

**Предусловие:** У тебя есть 1 личность из Теста 2

**Шаги:**
1. Выйди из группы проекта (временно)

2. Активируй Pro через админскую команду:
   ```
   /grantpro ТВОЙ_USER_ID 30
   ```

3. Проверь статус: `/mystatus`
   ```
   📊 Твой статус

   Тариф: 💎 Pro
   Активен до: 2025-12-17
   ...
   ```

4. Отправь `/lichnost`

5. Нажми **"➕ Создать личность"**

**Ожидаемый результат:**
- ✅ Процесс создания начался (бот просит название)

**Продолжение:**
6. Создай 2-ю личность (например, "Повар")
7. Создай 3-ю личность (например, "Детектив")
8. Попробуй создать 4-ю

**Ожидаемый результат при 4-й попытке:**
```
⚠️ Лимит кастомных личностей: 3/3

Вступи в группу проекта для получения +1 слота!
```

**Проверка в БД:**
```sql
SELECT name, is_group_bonus
FROM personalities
WHERE created_by_user_id = ТВОЙ_USER_ID
ORDER BY created_at;
```

**Ожидаемые данные:**
| name | is_group_bonus |
|------|----------------|
| космонавт | true |
| повар | false |
| детектив | false |

**Проверка:**
- ✅ Создано 3 личности
- ✅ Первая (космонавт) - бонусная (`is_group_bonus = true`)
- ✅ Вторая и третья - обычные (`is_group_bonus = false`)
- ✅ 4-ю создать нельзя

---

### Тест 5: Pro пользователь в группе - максимум 4 личности 💎✨

**Цель:** Проверить что Pro + группа = 4 слота

**Предусловие:** Выполнен Тест 4, создано 3 личности

**Шаги:**
1. Вступи обратно в группу проекта

2. Подожди 1-2 минуты (обновление кеша)

3. Отправь `/lichnost`

4. Нажми **"➕ Создать личность"**

**Ожидаемый результат:**
- ✅ Процесс создания начался

**Продолжение:**
5. Создай 4-ю личность (например, "Философ-дзен")

6. Попробуй создать 5-ю

**Ожидаемый результат:**
```
⚠️ Достигнут максимум: 4/4

Удали неиспользуемые личности через /lichnost
```

**Проверка:**
- ✅ Создано 4 личности
- ✅ 5-ю создать нельзя
- ✅ Сообщение говорит об удалении неиспользуемых

---

### Тест 6: Выход из группы - блокировка бонусной личности 🔒

**Цель:** Проверить что при выходе из группы бонусная личность блокируется

**Предусловие:** Выполнен Тест 2, создана бонусная личность "космонавт"

**Шаги:**
1. Убедись что личность "космонавт" доступна:
   - Отправь `/lichnost`
   - В списке должна быть "космонавт" без замка

2. Выйди из группы проекта

3. Подожди 1-2 минуты

4. Проверь в БД:
   ```sql
   SELECT name, is_blocked, is_group_bonus
   FROM personalities
   WHERE name = 'космонавт';
   ```

**Ожидаемый результат в БД:**
- `is_blocked` = `true`
- `is_group_bonus` = `true`

5. Отправь `/lichnost`

**Ожидаемый результат:**
- ✅ Личность "космонавт" показана с замком: "🔒 Космонавт"
- ✅ Или вообще не показана в списке

6. Попробуй выбрать эту личность (если показана)

**Ожидаемый результат:**
```
⚠️ Эта личность заблокирована. Вернись в группу проекта!
```

**Проверка:**
- ✅ Бонусная личность заблокирована
- ✅ Показано сообщение о причине
- ✅ В БД `is_blocked = true`

---

### Тест 7: Возврат в группу - разблокировка 🔓

**Цель:** Проверить что при возврате в группу личность разблокируется

**Предусловие:** Выполнен Тест 6, личность "космонавт" заблокирована

**Шаги:**
1. Вступи обратно в группу проекта

2. Подожди 1-2 минуты

3. Проверь в БД:
   ```sql
   SELECT name, is_blocked
   FROM personalities
   WHERE name = 'космонавт';
   ```

**Ожидаемый результат:**
- `is_blocked` = `false`

4. Отправь `/lichnost`

**Ожидаемый результат:**
- ✅ Личность "космонавт" показана БЕЗ замка
- ✅ Можно выбрать и использовать

5. Выбери личность "космонавт"

6. Проверь что она работает: отправь `/summary` в группе, выбери "космонавт"

**Проверка:**
- ✅ Личность разблокирована
- ✅ Работает корректно
- ✅ В БД `is_blocked = false`

---

## 🔍 Проверка в БД после всех тестов

После завершения всех тестов, проверь состояние данных:

```sql
-- 1. Проверить подписку
SELECT user_id, tier, is_active, expires_at
FROM subscriptions
WHERE user_id = ТВОЙ_USER_ID;

-- 2. Проверить членство в группе
SELECT user_id, is_member, checked_at
FROM group_membership_cache
WHERE user_id = ТВОЙ_USER_ID;

-- 3. Проверить все личности
SELECT name, display_name, is_custom, is_group_bonus, is_blocked, created_by_user_id
FROM personalities
WHERE created_by_user_id = ТВОЙ_USER_ID
ORDER BY created_at;

-- 4. Проверить счетчик активных личностей
SELECT COUNT(*) as active_count
FROM personalities
WHERE created_by_user_id = ТВОЙ_USER_ID
  AND is_custom = true
  AND is_active = true;
```

---

## ✅ Чек-лист успешного тестирования

После прохождения всех тестов, отметь:

- [ ] Тест 1: Free без группы - создание запрещено ✅
- [ ] Тест 2: Free + группа - создана 1 бонусная личность ✅
- [ ] Тест 3: Free + группа - 2-ю создать нельзя ✅
- [ ] Тест 4: Pro без группы - создано 3 личности ✅
- [ ] Тест 5: Pro + группа - создано 4 личности ✅
- [ ] Тест 6: Выход из группы - бонусная личность заблокирована ✅
- [ ] Тест 7: Возврат в группу - личность разблокирована ✅

---

## 🐛 Что делать если тест провалился?

### Проблема: Создание началось когда не должно было

**Диагностика:**
1. Проверь что функция `can_create_custom_personality()` вызывается в `personalities.py:111`
2. Проверь что `get_custom_personality_limit()` возвращает правильный лимит
3. Проверь логи бота:
   ```bash
   # В Vercel
   vercel logs
   ```

**Возможные причины:**
- Не вызывается проверка лимитов
- Кеш группы устарел (старше 1 часа)
- Тариф определяется неправильно

---

### Проблема: `is_group_bonus` всегда `false`

**Диагностика:**
1. Проверь что в `personalities.py:344` определяется тариф
2. Проверь логи создания личности:
   ```
   Created personality 'космонавт' (ID: 123, group_bonus: true)
   ```

**Возможные причины:**
- Не передается параметр `is_group_bonus` в `create_personality()`
- Тариф определяется как Pro вместо Free

---

### Проблема: Блокировка не работает

**Диагностика:**
1. Проверь что ChatMemberHandler зарегистрирован в `api/index.py`
2. Проверь что Privacy Mode выключен в BotFather
3. Проверь что `PROJECT_TELEGRAM_GROUP_ID` установлен правильно
4. Проверь логи при выходе из группы

**Возможные причины:**
- `chat_member` обновления не приходят (Privacy Mode включен)
- Неправильный ID группы
- Обработчик не зарегистрирован

---

## 📝 Полезные SQL-запросы для отладки

### Сбросить тариф на Free
```sql
DELETE FROM subscriptions WHERE user_id = ТВОЙ_USER_ID;
```

### Активировать Pro на 30 дней
```sql
INSERT INTO subscriptions (user_id, tier, expires_at, is_active, payment_method)
VALUES (
  ТВОЙ_USER_ID,
  'pro',
  NOW() + INTERVAL '30 days',
  true,
  'manual'
)
ON CONFLICT (user_id)
DO UPDATE SET
  tier = 'pro',
  expires_at = NOW() + INTERVAL '30 days',
  is_active = true,
  updated_at = NOW();
```

### Вручную установить членство в группе
```sql
INSERT INTO group_membership_cache (user_id, is_member, checked_at)
VALUES (ТВОЙ_USER_ID, true, NOW())
ON CONFLICT (user_id)
DO UPDATE SET is_member = true, checked_at = NOW();
```

### Разблокировать все личности
```sql
UPDATE personalities
SET is_blocked = false
WHERE created_by_user_id = ТВОЙ_USER_ID;
```

### Удалить все кастомные личности (начать заново)
```sql
DELETE FROM personalities
WHERE created_by_user_id = ТВОЙ_USER_ID
  AND is_custom = true;
```

---

## 🎓 Советы для тестирования

1. **Используй отдельный тестовый аккаунт** если возможно
2. **Проверяй БД после каждого действия** - это поможет понять что пошло не так
3. **Делай скриншоты** сообщений бота для документации
4. **Тестируй последовательно** - не пропускай тесты
5. **Используй SQL-запросы** для быстрого изменения состояния
6. **Проверяй логи Vercel** если что-то не работает

---

## 📞 Что делать если нужна помощь

Если тесты не проходят:

1. Собери информацию:
   - Какой тест провалился
   - Что ожидалось vs что произошло
   - Скриншот сообщения бота
   - Результаты SQL-запросов
   - Логи Vercel (если есть доступ)

2. Проверь:
   - Применены ли все миграции БД
   - Установлены ли environment variables в Vercel
   - Перезапущен ли бот после изменений

3. Используй отладочные SQL-запросы выше для сброса состояния

---

**Удачи в тестировании! 🚀**
