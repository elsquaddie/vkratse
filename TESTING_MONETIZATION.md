# 🧪 Инструкция по тестированию монетизации

**Версия:** v2.1
**Дата:** 2025-11-17
**Статус:** Базовая инфраструктура готова

---

## 📋 Содержание

1. [Текущий статус](#текущий-статус)
2. [Тестирование СЕЙЧАС (ручное управление)](#тестирование-сейчас-ручное-управление)
3. [Тестирование ПОТОМ (с платежами)](#тестирование-потом-с-платежами)
4. [Админские команды](#админские-команды)
5. [Типовые сценарии тестирования](#типовые-сценарии-тестирования)
6. [Проверка БД](#проверка-бд)
7. [Troubleshooting](#troubleshooting)

---

## 🎯 Текущий статус

### ✅ Что уже реализовано:

- ✅ Таблицы БД: `subscriptions`, `usage_limits`, `personality_usage`, `group_membership_cache`
- ✅ Сервис `services/subscription.py` с логикой тарифов и лимитов
- ✅ Методы в `db_service.py` для работы с подписками
- ✅ Админские команды для ручного управления (`modules/admin.py`)
- ✅ Конфигурация `TIER_LIMITS` в `config.py`

### ❌ Что еще НЕ реализовано:

- ❌ Интеграция проверки лимитов в обработчики команд
- ❌ Команды `/premium`, `/mystatus` для пользователей
- ❌ Проверка личностных лимитов (для Free-пользователей)
- ❌ Бонус за группу (кастомная личность)
- ❌ Автоматические платежи (Telegram Stars, YooKassa, Tribute)

---

## 🛠️ Тестирование СЕЙЧАС (ручное управление)

### Шаг 1: Настройка окружения

#### 1.1 Установить ADMIN_USER_ID

В файле `.env` добавьте ваш Telegram ID:

```bash
ADMIN_USER_ID=123456789  # Замените на свой Telegram ID
```

**Как узнать свой Telegram ID:**

1. Отправьте боту команду `/start`
2. Посмотрите в логи Vercel:
   ```
   User 123456789 started the bot
   ```
3. Это и есть ваш `user_id`

#### 1.2 Применить миграции БД

Убедитесь, что все миграции из `sql/migrations/` применены в Supabase:

- `003_monetization_tables.sql` - основные таблицы
- `004_group_bonus.sql` - поле `is_group_bonus`

**Как применить:**

1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое миграции и выполните
4. Проверьте, что таблицы созданы

#### 1.3 Обновить handlers (временно)

Пока интеграция не завершена, нужно зарегистрировать админские команды вручную.

В файле `api/index.py` добавьте:

```python
from modules.admin import (
    setplan_command,
    checkplan_command,
    resetusage_command,
    adminhelp_command
)

# В функции handler(), после регистрации других команд:
application.add_handler(CommandHandler('setplan', setplan_command))
application.add_handler(CommandHandler('checkplan', checkplan_command))
application.add_handler(CommandHandler('resetusage', resetusage_command))
application.add_handler(CommandHandler('adminhelp', adminhelp_command))
```

#### 1.4 Инициализировать subscription_service

В `api/index.py`, после создания `db_service`:

```python
from services.subscription import init_subscription_service

# После db_service = DBService()
sub_service = init_subscription_service(db_service)
```

---

### Шаг 2: Запуск админских команд

#### 2.1 Проверить список команд

Отправьте боту:

```
/adminhelp
```

**Ожидаемый результат:**

Бот покажет список всех админских команд с примерами.

Если получили "⛔ Доступ запрещен" → проверьте, что `ADMIN_USER_ID` установлен правильно.

#### 2.2 Установить Pro-план себе

```
/setplan 123456789 pro 30
```

Замените `123456789` на ваш реальный user_id.

**Ожидаемый результат:**

```
✅ Подписка установлена!

User ID: 123456789
Тариф: 💎 Pro
Срок: 30 дней
Истекает: 2025-12-17

✅ Пользователь уведомлен
```

И вы получите личное сообщение от бота:

```
🎉 Поздравляем!

Вам активирована Pro-подписка на 30 дней.
Наслаждайтесь безлимитными возможностями!

Проверить статус: /mystatus
```

#### 2.3 Проверить план

```
/checkplan 123456789
```

**Ожидаемый результат:**

```
📊 Информация о пользователе 123456789

Тариф: 💎 Pro
Активна: ✅ Да
Истекает: 2025-12-17 23:59:59 UTC
Осталось: 30 дней
Способ оплаты: manual

📈 Использование сегодня: 0 (нет активности)
```

---

### Шаг 3: Тестирование смены планов

#### 3.1 Установить Free-план

```
/setplan 123456789 free 0
```

**Ожидаемый результат:**

```
✅ Подписка установлена!

User ID: 123456789
Тариф: 🆓 Free
Срок: 0 дней
```

#### 3.2 Проверить изменение

```
/checkplan 123456789
```

Должно показать `Тариф: 🆓 Free`.

---

### Шаг 4: Тестирование лимитов (когда интегрировано)

⚠️ **Пока НЕ работает** - требуется интеграция в обработчики команд.

Когда будет реализовано:

1. Установить себе Free план
2. Отправить боту 31 сообщение в ЛС
3. На 31-м должно появиться уведомление о превышении лимита
4. Установить Pro план
5. Отправить 500+ сообщений - все должны работать

---

### Шаг 5: Сброс лимитов (для повторного тестирования)

```
/resetusage 123456789
```

**Ожидаемый результат:**

```
✅ Лимиты сброшены для пользователя 123456789

Все счетчики обнулены на 2025-11-17
```

Теперь можно снова тестировать лимиты с нуля.

---

## 🚀 Тестирование ПОТОМ (с платежами)

### Когда будет реализована интеграция с платежными системами

#### Tribute.to (ручная активация)

1. Пользователь открывает `/premium`
2. Нажимает "🎁 Donate (Tribute.to)"
3. Переходит на страницу Tribute, делает донат
4. Присылает скриншот админу
5. Админ активирует через `/setplan <user_id> pro 30`

#### YooKassa (автоматическая активация)

1. Пользователь открывает `/premium`
2. Нажимает "💳 Купить Pro (Карта)"
3. Переходит на страницу оплаты YooKassa
4. Вводит данные карты, оплачивает
5. **Webhook автоматически активирует подписку**
6. Пользователь получает уведомление

**Тестирование webhook:**

```bash
# Отправить тестовый webhook (из локального окружения)
curl -X POST https://vkratse.vercel.app/api/yookassa_webhook \
  -H "Content-Type: application/json" \
  -d '{
    "event": "payment.succeeded",
    "object": {
      "id": "test_payment_123",
      "metadata": {
        "user_id": "123456789",
        "tier": "pro",
        "duration_days": "30"
      }
    }
  }'
```

После этого проверить:

```
/checkplan 123456789
```

Должна появиться активная Pro-подписка.

#### Telegram Stars (автоматическая активация)

1. Пользователь открывает `/premium`
2. Нажимает "⭐ Купить Pro (Stars)"
3. Бот отправляет invoice
4. Пользователь оплачивает через Telegram
5. **Бот получает `SuccessfulPayment` и активирует подписку**
6. Пользователь получает уведомление

**Тестирование:**

- Telegram предоставляет тестовые токены Stars для разработчиков
- См. [документацию Telegram Payments](https://core.telegram.org/bots/payments)

---

## 🔧 Админские команды

### `/setplan <user_id> <tier> [days]`

**Назначение:** Вручную установить тарифный план пользователю.

**Параметры:**

- `user_id` (обязательный) - Telegram ID пользователя
- `tier` (обязательный) - `'free'` или `'pro'`
- `days` (опциональный) - Количество дней (default: 30 для pro, 0 для free)

**Примеры:**

```bash
# Дать Pro на 30 дней
/setplan 123456789 pro 30

# Дать Pro на 7 дней (тестовый период)
/setplan 123456789 pro 7

# Вернуть на Free
/setplan 123456789 free 0
```

**Что происходит:**

1. Создается/обновляется запись в таблице `subscriptions`
2. Устанавливается `expires_at` = NOW + `days`
3. Пользователь получает уведомление в ЛС
4. Админ получает подтверждение

---

### `/checkplan <user_id>`

**Назначение:** Проверить текущий тариф и использование лимитов.

**Параметры:**

- `user_id` (обязательный) - Telegram ID пользователя

**Пример:**

```bash
/checkplan 123456789
```

**Что показывает:**

- Текущий тариф (Free/Pro)
- Активность подписки
- Дата истечения
- Количество оставшихся дней
- Способ оплаты
- Использование лимитов за сегодня:
  - Сообщения
  - Саммари (ЛС)
  - Саммари (группы)
  - Судейство

---

### `/resetusage <user_id>`

**Назначение:** Сбросить лимиты использования за сегодня (для повторного тестирования).

**Параметры:**

- `user_id` (обязательный) - Telegram ID пользователя

**Пример:**

```bash
/resetusage 123456789
```

**Что происходит:**

1. Удаляется запись из `usage_limits` за сегодня
2. Удаляются записи из `personality_usage` за сегодня
3. Все счетчики обнуляются

**Когда использовать:**

- После тестирования лимитов Free-тарифа
- Когда нужно протестировать еще раз "с нуля"
- Для помощи пользователям (если была ошибка)

---

### `/adminhelp`

**Назначение:** Показать список всех админских команд с примерами.

**Пример:**

```bash
/adminhelp
```

---

## 📝 Типовые сценарии тестирования

### Сценарий 1: Проверка базовой работы

1. Установить себе Free план:
   ```
   /setplan <your_id> free 0
   ```
2. Проверить план:
   ```
   /checkplan <your_id>
   ```
   Должно показать Free.
3. Установить себе Pro на 7 дней:
   ```
   /setplan <your_id> pro 7
   ```
4. Проверить план:
   ```
   /checkplan <your_id>
   ```
   Должно показать Pro с датой истечения через 7 дней.

---

### Сценарий 2: Тестирование истечения подписки (когда реализовано)

1. Установить Pro на 1 день:
   ```
   /setplan <your_id> pro 1
   ```
2. **Вручную в БД** изменить `expires_at` на вчерашнюю дату:
   ```sql
   UPDATE subscriptions
   SET expires_at = NOW() - INTERVAL '1 day'
   WHERE user_id = <your_id>;
   ```
3. Отправить любую команду боту (например, `/start`)
4. Проверить план:
   ```
   /checkplan <your_id>
   ```
   Должно показать Free (автоматический даунгрейд).

---

### Сценарий 3: Тестирование лимитов Free (когда реализовано)

1. Установить Free план
2. Отправить боту 30 сообщений в ЛС
3. На 31-м сообщении должно появиться:
   ```
   ⚠️ Лимит сообщений исчерпан (30/30).
   Обновите тариф: /premium
   ```
4. Сбросить лимиты:
   ```
   /resetusage <your_id>
   ```
5. Установить Pro план
6. Отправить 100 сообщений - все должны работать

---

### Сценарий 4: Тестирование личностных лимитов (когда реализовано)

1. Установить Free план
2. Использовать личность "Быдлан" для summary 5 раз
3. На 6-й раз должно появиться:
   ```
   ⚠️ Лимит использования личности 'bydlan' исчерпан (5/5).

   💎 Pro-подписка дает безлимитное использование всех личностей!
   Узнать больше: /premium
   ```
4. Установить Pro план
5. Использовать "Быдлан" 20+ раз - все должны работать без лимита

---

## 🗄️ Проверка БД

### Просмотр подписок

```sql
SELECT * FROM subscriptions ORDER BY created_at DESC LIMIT 10;
```

**Что смотреть:**

- `user_id` - ID пользователя
- `tier` - тариф ('free' или 'pro')
- `is_active` - активна ли подписка
- `expires_at` - дата истечения
- `payment_method` - способ оплаты

---

### Просмотр использования лимитов

```sql
SELECT * FROM usage_limits
WHERE date = CURRENT_DATE
ORDER BY user_id;
```

**Что смотреть:**

- `messages_count` - количество сообщений сегодня
- `summaries_dm_count` - саммари в ЛС
- `summaries_count` - саммари в группах
- `judge_count` - судейство

---

### Просмотр использования личностей

```sql
SELECT * FROM personality_usage
WHERE date = CURRENT_DATE
ORDER BY user_id, personality_name;
```

**Что смотреть:**

- `personality_name` - название личности
- `summary_count` - использования для summary
- `chat_count` - использования для chat
- `judge_count` - использования для judge

---

## 🚨 Troubleshooting

### Проблема: "⛔ Доступ запрещен" на админские команды

**Причина:** `ADMIN_USER_ID` не установлен или неправильный.

**Решение:**

1. Проверьте `.env`:
   ```bash
   ADMIN_USER_ID=<your_real_telegram_id>
   ```
2. Узнайте свой ID через логи или [@userinfobot](https://t.me/userinfobot)
3. Обновите переменную в Vercel Dashboard:
   - Settings → Environment Variables
   - Измените `ADMIN_USER_ID`
   - Redeploy

---

### Проблема: "❌ Subscription service не инициализирован"

**Причина:** Не вызван `init_subscription_service()` в `api/index.py`.

**Решение:**

Добавьте в `api/index.py`:

```python
from services.subscription import init_subscription_service

# После создания db_service
sub_service = init_subscription_service(db_service)
```

---

### Проблема: "⚠️ Не удалось уведомить пользователя"

**Причина:** Пользователь не запускал бота (нет чата с ботом).

**Решение:**

- Это нормально для новых пользователей
- Попросите пользователя сначала отправить боту `/start`
- После этого повторите `/setplan`

---

### Проблема: Лимиты не работают

**Причина:** Интеграция в обработчики команд еще не завершена.

**Решение:**

- Дождитесь реализации Этапа 2 и 3 из TODO
- Пока тестируйте только ручное управление планами
- Лимиты будут работать после интеграции `check_usage_limit()` в handlers

---

### Проблема: БД не содержит таблицы

**Причина:** Миграции не применены.

**Решение:**

1. Откройте Supabase Dashboard
2. SQL Editor
3. Выполните миграции из `sql/migrations/`:
   - `003_monetization_tables.sql`
   - `004_group_bonus.sql`
4. Проверьте через SQL Editor:
   ```sql
   SELECT table_name FROM information_schema.tables
   WHERE table_schema = 'public'
   AND table_name IN ('subscriptions', 'usage_limits', 'personality_usage', 'group_membership_cache');
   ```

---

## 📚 Дополнительные ресурсы

- **TODO_MONETIZATION_v2.1.md** - полный план реализации
- **CLAUDE.md** - документация проекта
- **sql/migrations/README_MONETIZATION_MIGRATIONS.md** - инструкции по миграциям

---

## ✅ Чеклист готовности к тестированию

Перед началом убедитесь:

- [ ] `ADMIN_USER_ID` установлен в `.env` и Vercel
- [ ] Миграции БД применены в Supabase
- [ ] Админские команды зарегистрированы в `api/index.py`
- [ ] `subscription_service` инициализирован
- [ ] Бот задеплоен на Vercel
- [ ] Вы знаете свой Telegram ID
- [ ] У вас есть доступ к Supabase Dashboard

---

**Последнее обновление:** 2025-11-17
**Версия документа:** 1.0
